# VITRIOL Session Timeline — May 19-20, 2026

## 2026-05-19

### ~15:00 — MTP Draft-N-Max Sweep
- Sweep: N=2,3,4,6,8,12 against Qwen3.6-35B-A3B-UD-IQ2_M (Unsloth)
- **Result: N=2 optimal** at 10.96 tok/s (llama-bench)
- Acceptance rate exactly 1/N (confirmed: 63 accepted / 126 generated for N=2)
- Sweep script committed as `scripts/benchmark_mtp_sweep.sh`

### ~16:00 — MTP Auto-Detection + TUI
- `model_has_mtp()` reads GGUF header for `nextn_predict_layers`
- TUI options 7/8: Spec type (mtp/draft/off) and Draft N-Max
- TUI indicator shows `[MTP]` next to model name

### ~17:00 — Bugfixes
- `write_config` missing `kv.quant_mode` — template didn't emit quant_mode, `config set` silently dropped to F16
- `model_has_mtp()` pipefail+SIGPIPE — switched to process substitution
- README updated with 10.96 tok/s header, PCIe x16 warning, LRU section marked inactive

### ~18:00 — Merged feat/mtp-experiment → main
- Fast-forward merge

### ~18:30 — Created feat/fate-prefetch branch
- For predictor + prefetch experiments

### ~19:00 — Cross-Layer + Temporal Predictor
- Per-layer expert tracking (128 layers × 256 experts)
- Token boundary detection (wraparound detection)
- Atexit stats dump
- Stats reset bug fixed (`static bool first_init`)

### ~20:00 — LRU Cache Active Testing
- 49% hit rate without predictor
- 34% hit rate with predictor (predictor overhead reduces time for cache accumulation)
- Confirmed: sorted path is slower than MMV path regardless of LRU

### ~21:00 — Option 1: Fewer Active Experts
- `n_expert_used_override` in `llama_cparams`, threaded through graph context
- `VITRIOL_EXPERT_COUNT` env var
- **Result: CRASH** — graph shape mismatch when n_expert_used differs from model default
- `GGML_ASSERT(view_src == NULL || data_size == 0 || data_size + view_offs <= ggml_nbytes(view_src))`

### ~22:00 — Option 2: Expert Output Cache
- Per-layer per-expert VRAM cache (128 × 256 × 2048 floats = 256 MB)
- Forces sorted path when active during single-token generation
- Cache lookup before per-expert matmul, store after compute
- **Benchmark: 8.56 t/s** vs baseline 8.73 t/s — net loss (sorted path overhead > cache benefit)

### ~22:30 — Config Defaults Reset
- `model.expert_count=0`, `vitriol.output_cache=off`
- `spec.type=""`, `spec.draft_n_max=0`
- `model.path` restored to Q2_K_XL

### ~23:00 — Pushed Everything
- VITRIOL `feat/fate-prefetch` → `main` merged and pushed
- Fork branch `feat/fewer-experts-output-cache` pushed
- Fork `vitriol` branch fast-forwarded

### ~23:30 — Script Bugfix
- `local mtp_flag=""` outside function (line ~1124) — `local: can only be used in a function` on `serve --detach`
- Fixed by removing `local`
- Also missing EXPERT_COUNT/OUTPUT_CACHE var mapping in serve path — added

### ~23:45 — MTP N=2 Server-Log Benchmark (Post-Opt)
- Via server log, single request per measurement:
  - n=50: **10.02 t/s**
  - n=100: **10.38 t/s**

## 2026-05-20

### ~00:00 — Backward Compatibility Verified
- Checked: no original code deleted; all changes wrapped in disabled-by-default conditionals

### ~00:15 — Disk Offload Confirmed
- Already exists via `vitriol-buffer.cpp` file-backed mmap path
- No `mlock` on disk-offload path (pages can be evicted by OS)

### ~00:30 — Pre-Optimization Benchmark Attempt 1
- Switched to `bench/pre-optimization` branch (VITRIOL `62fa31f`, submodule `4da2973da`)
- Built from existing `build/` directory
- Attempted manual server launch with `--speculative-model mtp` — **WRONG FLAG**
- **CRASHED PC** — used `--mlock` + `--no-mmap` without `-fa on` → OOM + hard freeze → forced reset

### ~01:00 — Pre-Optimization Benchmark Attempt 2
- **CRASHED PC** — same manual command killed the system again

### ~01:30 — Root-Cause Analysis of Crashes
- Identified: my manual commands missing critical defaults:
  - `-fa on` (flash attention, required for V cache quant)
  - `--cache-type-k/v q4_0`
  - Wrong flag name (`--speculative-model` vs `--spec-type`)
  - Wrongly added `--mlock` (not in script's defaults)
- **Lesson**: Always use the script to launch the server

### ~02:30 — Pre-Optimization Benchmark Succeeded
- Fixed `local` bug in pre-opt script (same bug existed at `62fa31f`)
- Launched via `vitriol serve --detach` with correct script defaults
- **Results:**
  - n=50: **9.96 t/s** (post-opt: 10.02 t/s — +0.6%)
  - n=100: **9.80 t/s** (post-opt: 10.38 t/s — +5.9%)
- **Conclusion: No regression detected.** Small differences within server-log measurement variance (~5-10%). All optimization changes are additive-only and disabled-by-default.

### ~03:00 — Cleanup + Restore main branch
- Killed server, checked out `main`, submodule at `68517ef9a`

### ~03:15 — Setup Check (Hard Error)
- Changed CAP_IPC_LOCK check from WARNING to ERROR + exit 1 in both `run` and `serve` commands
- Forces user to run `vitriol setup` before launch

### ~03:20 — Default Context Window Update
- `DEFAULT_CTX=4096` → `DEFAULT_CTX=262144` (optimal for Qwen3.6 per web research)
- opencode profile at `~/.config/opencode/opencode.jsonc` already had `"context": 262144` — confirmed

### ~03:30 — Graceful Teardown (fsck Prevention)
- Diagnosis via `journalctl -b -1 -e`: NVIDIA driver DMA death grip on shutdown
- `vitriol-buffer.cpp`:
  - Added `munlock()` call in `vitriol_buffer_free` (belt-and-suspenders, `munmap` implicitly unlocks but explicit is safer)
  - Added atexit guard `vitriol_buffer_atexit_cleanup()` that catches cases where `.free_buffer` isn't called
  - Prints `"VITRIOL: Memory locks released. Safe to shutdown."` on cleanup
- `vitriol-cuda-integration.cpp`:
  - Added `vitriol_cuda_cleanup_vram()` that frees `g_lru_pool` and `g_output_cache_pool` (VRAM leak fix)
  - Registered via `atexit` alongside stats dump
- `scripts/vitriol`:
  - Added `sleep 2` after SIGTERM in `vitriol stop` to give server time for DMA cleanup

## Key Metrics

| Metric | Value | Context |
|--------|-------|---------|
| Best throughput (MTP N=2) | 10.96 tok/s | llama-bench, IQ2_M |
| Server-log MTP N=2 (post-opt) | 10.02-10.38 t/s | HTTP overhead |
| Server-log MTP N=2 (pre-opt) | 9.80-9.96 t/s | No regression confirmed |
| Non-MTP baseline | 8.73 t/s | MMV path, 8 experts |
| Output Cache | 8.56 t/s | Net loss with sorted path |
| Expert Count=4 | CRASH | Graph shape mismatch |
| Q4_0 KV at 256K | ~3.2 GiB | vs ~12 GiB for F16 |

## Known Issues
1. **Expert Count=4**: GGML_ASSERT crash — graph shape mismatch when `n_expert_used` differs from model default
2. **Output Cache**: 8.56 t/s net loss — sorted path overhead > cache benefit. Requires MMV kernel per-expert pointer table to be useful
3. **Predictor LRU**: Sorted path (per-expert loop) is slower than MMV kernel for generation. Any benefit requires MMV kernel modifications
4. **Pre-opt `bench/pre-optimization` branch**: Has the `local mtp_flag=""` bug. We fixed it during testing but didn't commit the fix

## Open Questions
- [ ] True MMV kernel LRU support via per-expert pointer tables?
- [ ] Is the atexit guard in vitriol-buffer.cpp sufficient for all shutdown paths?
- [ ] Should we delete `bench/pre-optimization` branch?

## Files Changed This Session
- `scripts/vitriol` — hard CAP check, sleep 2 on stop, DEFAULT_CTX=262144
- `vitriol-buffer.cpp` — munlock, atexit guard, cleanup messages
- `vitriol-cuda-integration.cpp` — VRAM pool atexit cleanup
- `~/.config/opencode/opencode.jsonc` — already correct (262144)

## Next Steps
1. Commit and push the teardown + setup-check changes
2. User runs `vitriol setup` to re-apply caps after rebuild
3. Verify "Safe to shutdown" message appears on `vitriol stop`
4. Monitor for fsck-free boots
