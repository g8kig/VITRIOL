## 4.5 calibrate.rs — Orchestration

```rust
pub fn calibrate_quick(model_path: &str, profile_name: &str, aggressive: bool) -> Result() -> Result<()> {
    // 1. hardware probe
    // 2. model analysis
    // 3. vram estimation
    // 4. display + save
}
```
